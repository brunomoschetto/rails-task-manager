<h1>Tasks</h1>

<p>These are your pending tasks: </p>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_details_path(task) %></li>
  <% end %>
</ul>

<h3><%= link_to "Create a new task", new_task_path %></h3>
